name: Dev Charter Check

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "48 6 * * 1"  # Every Mon 06:48 UTC
  workflow_dispatch:

jobs:
  check:
    if: github.actor != 'dependabot[bot]'
    uses: y-marui/dev-charter/.github/workflows/check-charter.yml@main
    with:
      prefix: docs/dev-charter
    permissions:
      contents: write
      pull-requests: write
      actions: read
